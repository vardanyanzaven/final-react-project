import { Box } from "@mui/material";
import { useState } from "react";

export const UserSettings = () => {
  const [isOpen, setIsOpen] = useState(true);
  return <Box>this is user settings</Box>;
};
